{
  "name": "node-util",
  "main": "bin/index.js",
  "version": "0.1.13",
  "scripts": {
    "install": "npm install livetex-tools && make && npm uninstall livetex-tools"
  },
  "dependencies": {
    "livetex-tools": ">=0.1.0"
  }
}
