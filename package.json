{
  "name": "node-util",
  "main": "bin/index.js",
  "version": "0.1.10",
  "scripts": {
    "prepublish": "npm install livetex-tools && make publish && npm uninstall livetex-tools"
  },
  "dependencies": {
    "livetex-tools": ">=0.1.0"
  }
}
