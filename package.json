{
  "name": "livetex-util",
  "main": "bin/index.js",
  "version": "1.0.0",

  "scripts": {
    "prepublish": "make clean && make check && make"
  },

  "dependencies": {
    "livetex-tools": "*"
  }
}
